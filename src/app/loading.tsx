export default function Loading() {
  return (
    <div className="ops-panel flex min-h-64 items-center justify-center px-4 text-sm font-semibold text-muted-foreground">
      加载状态：正在读取运营数据
    </div>
  );
}
